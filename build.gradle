apply plugin: 'java'

repositories {
    maven {
        url 'https://oss.sonatype.org/content/repositories/snapshots/'
    }
    mavenCentral()
}

dependencies {
    compile 'org.minetweak:Minetweak:0.6.8-SNAPSHOT'
}